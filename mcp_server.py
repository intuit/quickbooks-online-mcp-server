#!/usr/bin/env python3
"""
MCP Server for Quickbooks Online Mcp Server

Auto-generated by doc2db-integrate v2 (Phase 10).
Wraps 0 detected entry point(s) as MCP tools.

Usage:
    python mcp_server.py
"""
import asyncio
import json
import os
from pathlib import Path

from mcp.server.fastmcp import FastMCP

APP_DIR = Path(__file__).parent / "."
mcp = FastMCP("Quickbooks Online Mcp Server")


@mcp.tool()
async def health() -> str:
    """Check if the Quickbooks Online Mcp Server service is healthy."""
    return json.dumps({"status": "ok", "app_id": "quickbooks-online-mcp-server"})


if __name__ == "__main__":
    mcp.run()
