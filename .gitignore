node_modules/
dist/
*.tsbuildinfo

# Credentials never belong in this repository. The service receives the Intuit
# access token per request; it holds no long-lived QuickBooks credentials.
.env
.env.*
*.pem
.qbo-tokens.json
