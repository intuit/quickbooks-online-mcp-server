# Auto-generated by doc2db-integrate v2 (--target local)
# Local resource emulation for Quickbooks Online Mcp Server
services:
  quickbooks-online-mcp-server:
    build: .
    container_name: doc2db-quickbooks-online-mcp-server
    networks:
    - doc2db-network
    restart: unless-stopped
    environment:
      QUICKBOOKS_CLIENT_ID: ${QUICKBOOKS_CLIENT_ID:-}
      QUICKBOOKS_CLIENT_SECRET: ${QUICKBOOKS_CLIENT_SECRET:-}
      QUICKBOOKS_REALM_ID: ${QUICKBOOKS_REALM_ID:-}
      QUICKBOOKS_REDIRECTURI: ${QUICKBOOKS_REDIRECTURI:-}
      QUICKBOOKS_REFRESH_TOKEN: ${QUICKBOOKS_REFRESH_TOKEN:-}
networks:
  doc2db-network:
    external: true
